<nav class="navbar navbar-expand-md navbar-dark bg-primary">
  <a class="navbar-brand" href="/home">e-khôlle</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="nav navbar-nav mr-auto">
      <%if (role === 'admin') { %>
        <% include ../partialsAdmin/navAdmin %>
          <% } %>

            <%if (role === 'professeur') { %>
              <% include ../partialsProfesseur/navProfesseur %>
                <% } %>
    </ul>
    <ul class="nav navbar-nav ml-auto">
      <% if (typeof user!='undefined') { %>
        <li class="dropdown">
          <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
            <%= lycee %> :
              <%= user %>
          </a>
          <ul class="dropdown-menu">
            <li class="nav-item"><a data-toggle="modal" class="dropdown-item" href="#contactSupport">Contact Support</a></li>
            <li class="divider"></li>
            <li class="nav-item"><a class="dropdown-item" href="/users/logout">Logout</a></li>
          </ul>
        </li>
        <% } else{ %>
          <li class="dropdown">
            <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
              <%= lycee %>
            </a>
            <ul class="dropdown-menu">
              <li class="nav-item"><a data-toggle="modal" class="dropdown-item" href="#contactSupport">Contact Support</a></li>

            </ul>
          </li>
          <% }   %>
    </ul>
  </div>
</nav>
