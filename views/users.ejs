<!DOCTYPE html>
<html lang="fr">
  <head>
    <% include partials/head %>
  </head>
  <body>
    <header>
      <% include partials/header %>
    </header>
    <main>
    
      <div class="container">
        <div class="row">
          <% if (locals.messages) { %>
         <p style="color:red"><%=  locals.messages.msg  %></p>
          <% } %>
          <div id='login' class="visible">
            <form action="/users/login" method="post">
              <div class="form-group row">
                <label class="col-sm-4 col-form-label">Statut</label>
                <div class="col-sm-8">
                  <select name="statut">>
                    <option value="professeur">Professeur</option>
                    <option value="eleve">eleve</option>
                    <option value="admin">admin</option>
                  </select>
                </div>
              </div>
              <div class="form-group row">
                <label class="col-sm-4 col-form-label">login</label>
                <div class="col-sm-8">
                  <input class="form-control" type="text" name="login" placeholder="votre identifiant" required="">
                </div>
              </div>
              <div class="form-group row">
                <label class="col-sm-4 col-form-label">Mot de passe</label>
                <div class="col-sm-8">
                  <input class="form-control" type="password" name="logpassword" placeholder="Password" required="">
                </div>
              </div>
              <div class="form row">
                <div class="col-sm-6">
                  <button type="submit" class="btn btn-primary">LOGIN</button>
                </div>
                <div class="col-sm-6">
                  <a href="/users/forgot" >Mot de passe oublié</a>
                </div>
              </div>
            </form>
          </div>
        </div>
        <hr>
      </div>
    </main>
    <footer>
      <% include partials/footer %>
    </footer>
  </body>
</html>
